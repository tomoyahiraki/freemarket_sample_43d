%body
  .default-container
    - breadcrumb :search
    = render 'topheader'
    %main.white{role: "main"}
      .search-result
        .search-result_l-side
        .search-result_main
          %h2.search-result_main_text 検索結果
          - @products.each do |product|
            %section.items-box__contents
              %a{href: "/products/#{product.id}"}
                %figure.items-box__photo
                  .lazyloaded{:style => "background-image: url(#{product.images[0].image})"}
                .items-box__body
                  %h3.items-box__name.font-1
                    = product.title
                  .items-box__num.clearfix
                    .items-box__price.font-2
                      = product.price
                    %p.items-box__tax (税込)
    .clearbox
    = render 'topappbanner'
    = render 'topfooter'
    = render 'topcellbtn'
